pipeline {
    agent any
    stages {
        
        stage('Test Build') {
            steps {
                bat "mvn package"
            }
        }
        
        stage('Sonar') {
            steps {
                bat "mvn sonar:sonar"
            }
        }
	stage('Nexus') {
            steps {
                bat "mvn clean deploy -Dmaven.test.skip=true"
            }
        }
    }
    post {
        always {
	    emailext body: 'The Pipeline failed', subject: 'The Pipeline failed', to: 'rhoumainesiset@gmail.com'
        }
    }
    
}
